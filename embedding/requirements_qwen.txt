# Requirements for Qwen2.5 Text Embedding Extraction
# NOTE: Uses text-only model (AutoModelForCausalLM), NOT vision-language (VL).

# Core dependencies
torch>=2.0.0
transformers>=4.37.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # For quantization (4-bit / 8-bit)

# Data processing
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
