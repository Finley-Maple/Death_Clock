# Requirements for Qwen3-Embedding text embedding extraction
#
# Models:
#   Qwen/Qwen3-Embedding-0.6B  (1024-dim, CPU-friendly)
#   Qwen/Qwen3-Embedding-4B    (2560-dim, mid-range GPU)
#   Qwen/Qwen3-Embedding-8B    (4096-dim, GPU recommended)

# Core
torch>=2.4.0                      # transformers>=4.51.0 disables PyTorch if < 2.4
transformers>=4.53.0              # Qwen3Model architecture support
accelerate>=0.25.0

# Optional: fallback when transformers fails to load config (e.g. cache or local path conflict)
# pip install sentence-transformers
# sentence-transformers>=2.7.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
